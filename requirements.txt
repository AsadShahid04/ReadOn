# Core dependencies
openai==1.14.0
google-generativeai>=0.3.0
requests>=2.32.0
python-dotenv==1.0.1

# Image Processing
Pillow>=10.3.0  # Using standard Pillow instead of Pillow-SIMD

# Remove unnecessary dependencies
# pathlib is included in Python standard library, so we can remove it 